{
  "name": "baremoapp",
  "version": "5.8.39",
  "description": "BAREMOS - Registro de producción diaria por baremo",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "node server.ts",
    "build": "node build.js",
    "start": "node server.ts",
    "lint": "node --check app.js && node --check server.ts && node --check server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.2.0"
  }
}
